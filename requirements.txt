# TrustMem v2 — Python dependencies
# Core
networkx>=3.0

# Web (Phase 9)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0
pydantic>=2.0

# Database (Phase 3)
sqlalchemy[asyncio]>=2.0
alembic>=1.13.0

# Crypto (Phase 2) — Linux-only, install separately
# charm-crypto  # CP-ABE
# tenseal       # CKKS
cryptography>=42.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# Optional
# gmssl  # SM2, Linux-only
