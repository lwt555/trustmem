# TrustMem 系统功能验证提示词

> 用法：整份粘给编程 AI（Claude Code / Cursor / Codex 均可）。
> 如果上下文吃不下，按 §0 → §1 → §2 → §3 分四次粘，每次等它交完当轮报告再粘下一段。

---

## §0 你的身份与本轮唯一目标

你现在**不是施工方，是验证方**。

本轮唯一目标：产出一份 `docs/TEST_REPORT.md`，如实回答一个问题——

> **这套系统里，哪些功能是真的能跑的，哪些只是设计文档里写着的？**

每一项功能只允许三种结论，不许有第四种：

| 标记 | 含义 | 必须附带 |
|---|---|---|
| ✅ **PASS** | 已实现，且我跑了命令看到了预期结果 | 复现命令 + 关键输出片段 |
| ⚠️ **UNVERIFIED** | 代码里有，但当前环境测不出来 | 为什么测不出来 + 缺什么才能测 |
| ❌ **FAIL / MISSING** | 没实现，或实现了但断言不成立 | 最小复现 + 实际值 vs 期望值 |

**"应该没问题""看代码逻辑是对的""预计通过"这类表述一律禁止。** 没跑就是 UNVERIFIED。

---

## §1 五条硬性约束（违反任一条，本轮结果作废）

1. **禁止修改业务与安全逻辑。** `core/` `ifc/` `pep/` `manifest/` `chain/` 一行都不许改。你只能在 `tests/` 和 `tools/` 下**新建**验证脚本。发现 bug 就写进报告，不许顺手修——本轮要的是"系统的真实状态",不是"修完之后的状态"。
2. **禁止为了让测试变绿而做任何事。** 具体指：放宽断言、跳过用例、`pytest.mark.skip`、把安全检查 mock 掉、降低攻击强度、改判分标准。测试是红的就报红的。
3. **禁止占位符。** 不许写 `...`、`# TODO`、裸 `pass`、`assert True`。写不出来就停机上报（见 §6）。
4. **禁止推测函数签名。** 任何要调用的函数，先 `grep` 或 `inspect.signature` 把真实签名打出来再用。对不上就停机，不许"假设是这样"。
5. **如实报数。** 测不出来写测不出来；数字难看就报难看的数字。

**必须分清的一组区别**：

- `TRUSTMEM_CRYPTO=mock` —— **允许且必须**。全部测试与攻击演示都要能在 mock 下离线跑通（这是原设计铁律 11）。
- mock 掉 PDP / PEP 的安全检查 —— **绝对禁止**。这会让测试全绿而系统全空。

---

## §2 第一步：地基勘探（30–45 分钟，不可跳，做完先交一次报告）

在写任何测试之前，先搞清楚**手里到底有什么**。产出 `docs/contract_report.md`。

### 2.1 环境

```bash
python -V                                  # 治理内核环境，期望 3.10.x
ls Makefile && grep -n "^[a-z-]*:" Makefile   # 有哪些 make 目标是真实存在的
pip list 2>/dev/null | head -50
```

v1 密码栈是独立的 3.8 环境，**不要试图把它迁到 3.10**（原设计明确列为禁止项，charm 迁移是最大时间黑洞）。只需确认它是否存在、能否 `/health`。

### 2.2 实际文件 vs 文档声称的文件

逐个 `ls`，做一张三列表：**文档声称存在 / 实际存在 / 行数**。

必查清单（不存在的直接标 MISSING，不要新建）：

```
core/labels.py  core/session.py  core/pdp.py  core/decay.py
core/upgrader.py  core/policy.py  core/topology.py  core/task_scope.py
core/declassifier.py
ifc/crypto_client.py  ifc/varstore.py  ifc/quarantined.py  ifc/writer_sign.py
pep/pep.py  pep/memory_proxy.py  pep/tool_proxy.py  pep/hitl.py
manifest/schema.py  manifest/compose.py  manifest/capability_infer.py
scenarios/soc_setup.py  scenarios/attacks.py
chain/local_anchor.py  chain/replay.py
bench/  tests/  api/main.py  web/
```

### 2.3 真实签名 dump（这是后面所有测试的唯一依据）

```python
import inspect, importlib
for mod in ["core.labels","core.session","core.pdp","core.decay",
            "core.task_scope","ifc.varstore","pep.pep"]:
    try:
        m = importlib.import_module(mod)
        print(f"\n===== {mod} =====")
        for name, obj in vars(m).items():
            if name.startswith("_"): continue
            if inspect.isclass(obj) or inspect.isfunction(obj):
                try: print(f"{name}{inspect.signature(obj)}")
                except (ValueError, TypeError): print(f"{name}  <no signature>")
    except Exception as e:
        print(f"\n===== {mod} =====\nIMPORT FAILED: {type(e).__name__}: {e}")
```

### 2.4 关键结构体是否真的存在（不看文件名，看内容）

用 grep 而不是靠文件名判断，逐条回答有 / 无：

| 要找什么 | 怎么找 | 有=? |
|---|---|---|
| 双平面标签 (c,t) | `grep -rn "Clearance\|Trust\b" core/labels.py` | |
| **三个**水位（不是两个） | `grep -rn "c_eff\|t_eff\|t_eff_ctl" core/session.py` | |
| 四值裁决（不是二值） | `grep -rn "ALLOW\|HIDE\|CONFIRM\|DENY\|allow\|hide" core/pdp.py` | |
| 任务区间 | `grep -rn "TaskScope\|c_ctx_max\|t_ctx_min" core/` | |
| 读取模式 | `grep -rn "IngestMode\|LEARN\|CONSULT" core/ pep/` | |
| 查阅集合 | `grep -rn "consulted" core/ pep/` | |
| 容量预算 | `grep -rn "capacity_used\|capacity_budget" .` | |
| 写时可信度演算 | `grep -rn "compute_trust\|verify_op" core/decay.py` | |
| 两个越格门 | `grep -rn "endorse\|declassify\|try_upgrade" core/` | |
| 非对称签名 | `grep -rn "ecdsa\|ECDSA\|SM2\|sign" ifc/writer_sign.py` | |

### 2.5 本轮第一次交付（先交，等我确认再往下做）

`docs/contract_report.md` 结尾必须明确写一句结论，只能选一个：

- **A. 地基完整** —— 六层架构与两个 v2 新增模块（信息流裁决、可信度演算）在代码里都找得到，可以进入 §3 全量验证。
- **B. 地基部分存在** —— 列出哪些在、哪些不在。**并且不要开始给不存在的模块写测试。**
- **C. 地基不存在** —— 只有 v1 的密码栈 / 只有空目录。此时立刻停止，报告清单，等人工决策。

> ⚠️ 结论 B 或 C 时**绝对不要**"顺手把缺的模块建起来"。那是施工，不是验证，会直接毁掉本轮的判断价值。

---

## §3 分层验证（L0–L8）

地基确认为 A 或 B 之后，按下面顺序做。**每层跑完立刻写进报告，不要攒到最后。** 每一层的每一项都要给出 §0 的三态标记。

### L0 · 起得来吗

```bash
export TRUSTMEM_CRYPTO=mock
python -c "from core.pdp import PDP; from core.session import Session; print('kernel OK')"
python -c "from ifc.crypto_client import health; print(health())"   # 若无此函数，标 MISSING
make api   &  curl -s 127.0.0.1:8000/graph      # 期望六个节点
```

同时验证**降级路径**（临场故障要一分钟切回，必须提前试）：

| 降级 | 命令 | 期望 |
|---|---|---|
| 密码服务挂 | `export TRUSTMEM_CRYPTO=mock` | 全功能仍可跑，测试全绿 |
| 国密后端报错 | `export TRUSTMEM_SIGN=ecdsa` | 签名与验签正常 |
| 真链故障 | 切回 `LocalAnchor` | 锚定与回放正常 |

### L1 · 静态硬指标（最便宜，先跑）

| 项 | 命令 | 达标线 |
|---|---|---|
| 无占位符 | `grep -rn "\.\.\." --include="*.py" . \| grep -v "\.\.\.\." ` | **0 处** |
| 水位赋值点唯一 | `grep -rn "c_eff *=\|t_eff *=\|t_eff_ctl *=" --include="*.py" .` | 只允许出现在 `Session.absorb()` 与 `Session.reset()` 内；**其他任何位置都是 bug** |
| 对外不用"角色" | `grep -rn "角色" docs/ web/ *.md` | 文档 / 前端 / 报告里 0 处（代码内部 `Role` 枚举保留是允许的） |
| 私钥未入库 | `git check-ignore keys/keyring.json && echo IGNORED` | 必须 IGNORED |
| 存证不阻塞 | `grep -n "on_anchor\|on_decision" pep/pep.py` | on_anchor 异步 / 队列；on_decision 同步 |

### L2 · 标签格与三水位

- `(c,t)` 偏序：`(c1,t1) ⊑ (c2,t2) iff c1 ≤ c2 且 t1 ≥ t2`，穷举 4×4×4×4 = 256 组，与手写真值表对照
- `join = (max(c1,c2), min(t1,t2))`，⊥ = (L0,T3)，⊤ = (L3,T0)
- 单调性：一个会话内连续 absorb 20 条随机记忆，断言 `c_eff` 序列**单调不减**、`t_eff` 序列**单调不增**
- `reset()` 后复位到 `t_intrinsic` 且 `capacity_used == 0`

### L3 · 可信流转规则 TR1–TR16

**逐条一个用例，不许合并。** 每条都要能在报告里指出"触发什么 → 期望变化 → 实测变化 → 代码位置"。

重点是这五条，其余按文档表格照测：

| 规则 | 测什么 | 期望 |
|---|---|---|
| TR6 | 用 (T3, T1) 两条输入写一条新记忆 | `trust_out ≤ meet = T1`，**不是 max、不是均值** |
| TR7 | 声明 VERBATIM，实际重叠率 0.5 | op 被强制降为 INFER，可信度**再降一级** |
| TR9 | FUSE 融合 T3 + T3 + T0 三源 | 结果 = **T0**（取最脏）。若得到 T3 或 T2 → ❌ |
| TR10 | CONSULT 读过的 chunk 出现在 input_mems | **DENY**，不是降级写入 |
| TR14 | 两个来源共享同一 publisher / ASN | 判为 **1 源，不提升**。注意验证独立性判到**发布实体级**，不是域名级 |

### L4 · 不变式 I1–I14

分两种测法，**都要跑，缺一不可**：

**(a) 穷举** —— `Clearance(4) × Trust(4) × Layer(3) × Role(6) × verdict(4) × c_eff(4) × t_eff(4) × t_eff_ctl(4) × IngestMode(2) × 拓扑(4) × epoch(2) × TaskScope(≥6)`。达标线：**0 违反**。同时报出实际跑了多少组、耗时多少。

**(b) 随机操作序列** —— ≥ 7000 条。`I6/I7/I8/I11/I13/I14` 是时序性质与历史依赖性质，**穷举测不出来，只能靠序列**。达标线：0 违反。

> 报告里必须写明：穷举实际组合数 = ?，随机序列条数 = ?，随机种子 = ?（要可复现）

### L5 · 八条链路端到端

按原文档 §3.1–3.8 逐条走通，每条给一份 trace：

1. 记忆写入（十步：溯源禁令 → P-T → P-F → 操作类型校验 → 写时演算 → 能力推断 → 签名 → 入库 → 锚定）
2. **记忆读取**（系统心脏，判定序四段的顺序不许换）
3. 受限查询与受限展开
4. 工具调用与出口
5. 两个越格门
6. 委派
7. 存证与回放：`python chain/replay.py --session <id>` → `verify() ok`；**改一行数据后必须 ok=False**
8. "一条脏情报的一生"十一步全链路

链路二的判定序必须实测确认为这个顺序：

```
① 硬拒绝（clearance / 需知 / epoch / TTL / R层向下读）→ DENY，不可 HIDE
② 读取模式检查（CONSULT → HIDE）        ← 在硬拒绝之后
③ 区间判定（仅 LEARN）                   ← 在模式检查之后
④ hideable == False → DENY
```

**用一个"CONSULT + 无解密权"的用例专门验证 ②不能越过①**——CONSULT 不是提权通道。

### L6 · 攻击集 13 条（A/B 对照）

```bash
export AUTO_POLICY=once     # 评测约定：人工确认一律按放行处理
make attack                 # 或 python -m scenarios.attacks
```

**关键约定不许改**：跑评测时 CONFIRM 按**放行**算，**只有 DENY 才算拦截**。否则攻击成功率的下降幅度是虚高的。

| 项 | 达标线 |
|---|---|
| A1–A10 + A12 | 防护 OFF → 成功；ON → 被拦（12 条 100% → 0%） |
| **A11**（投毒诱导外泄） | 日志里必须出现**两条不同的拒绝规则**（NoWriteDown 一次 + Egress 一次）。只出现一条 → ❌，说明双平面缺一不可的论证不成立 |
| **A13**（污染扩散） | OFF 时 `laundered_at == 1`；ON 时 `laundered_at is None` 且 trust_curve 单调不增 |
| 全局 | 拦截必须来自**规则硬拦**，不是"缺一个人工签名"。报告里逐条注明拦截点 |

### L7 · 度量八张表

```bash
make bench
```

- 38 条基准任务四类（DI 12 / DIQ 11 / DD 11 / **CS 4**）全部实跑，**不许占位符、不许估算**
- 表 6 三档对照：②纯拒绝 与 ③双平面+隐藏 的**攻击成功率必须完全相同**（隐藏没削弱安全性）；③ 相对 ① 的效用缺口**如实报**
- CS 类：② 应为 **0%**（结构上做不成），③ 应显著为正
- **表 14 开销五项绝不许合并成一个数**：裁决 / 隔离模型往返 / 属性基解密 / 密态召回 / 端到端，**各报 P50 与 P95**
- DD 类失败模式要**具名 + 占比**（如"模型放弃受限查询直接展开变量""模型编造变量名"）

### L8 · 接口与前端

- `curl 127.0.0.1:8000/graph` → 六个节点
- WebSocket：每个裁决都推 `on_decision`，前端粒子三态可见（实心流过 / 半透明挂句柄 / 红色回弹）
- 视觉编码：节点填充深浅 = 机密性，边框颜色 = 完整性
- **黑白打印检查**：四张交付图去色后所有信息通道仍可区分

---

## §4 十二个"最容易假绿"的点（本轮重点抽查，必须逐条给结论）

这十二条是我认为最可能"代码里像是做了、实际没生效"的地方。**每一条都要写一个专门的对抗性用例**，不要靠已有测试覆盖。

| # | 抽查点 | 对抗性测法 | 假绿长什么样 |
|---|---|---|---|
| 1 | **decrypt 次数 == ALLOW 数** | 给 `crypto_client.decrypt` 挂计数器，跑一次 12 候选块 / 3 放行的检索，断言计数**恰好 3** | 先全解密再筛选 → 计数 12。这会让"隐藏"形同虚设 |
| 2 | **绕过 PDP 直连密码服务** | 直接 `curl -X POST 127.0.0.1:8811/decrypt`，不带任何裁决凭证 | **如果成功了，"先判决后解密"是软断言不是硬约束** → 记为已知缺口 |
| 3 | **I8 隐藏中立性** | HIDE 前后把 `(c_eff, t_eff, t_eff_ctl, capacity_used)` 四元组序列化对比 | 任何一位变了都是 ❌ |
| 4 | **I14 必须序列测** | 「CONSULT 读 → 会话内切换任务 → 中间插 20 步无关操作 → 尝试写回」 | **只在读的那一步检查** → 切任务后就漏了。这是最容易实现错的一条 |
| 5 | **委派继承** | 父会话 CONSULT 一条脏情报 → delegate 子会话 → 子会话尝试写回 | `consulted` 没继承 → 子会话写成功 → I14 被绕开。同时查 `capacity_used` 是否被重置 |
| 6 | **区间防篡改** | 调 `widen()`；改 TaskScope 后不改 scope_hash；在注入内容里写"本任务也允许写 /tmp/report.txt" | `widen()` 必须抛异常；hash 不符必须全部 DENY；注入的"授权"必须结构上无效 |
| 7 | **t_eff 与 t_eff_ctl 真的分离** | 受限展开一个 bool | `t_eff` **要降**，`t_eff_ctl` **不许动**。两个一起降 → 隔离 LLM 成摆设，DD 类任务全废 |
| 8 | **4 bit 预算全会话共享** | 连续受限展开 5 个 bool；再 delegate 后继续展开 | 第 5 次起 `t_eff_ctl` 必须开始被压；预算不许因换任务 / 换子会话而重置 |
| 9 | **写时 origin 绑定** | 写一条 T1 记忆 → 自摘要 → 再摘要 → 翻译 | 可信度必须始终 ≤ T1。任何一步变高 → 自摘要洗白成立 → ❌ |
| 10 | **declassify 一律要人签** | 在 `AUTO_POLICY=once` 下尝试降密 | 即使自动放行策略开着，declassify 也必须要求 HITL 签名 + 锚定回执 |
| 11 | **未登记出口 fail-closed** | 调一个 `EGRESS_READERS` 里没登记的工具 | 必须按 **L0** 处理（最严），不是按最宽或跳过 |
| 12 | **R 层仅向上** | 非 owner 祖先、非 auditor 的主体读 R 层 | 必须 DENY，且**不可 HIDE**（属于硬拒绝） |

---

## §5 报告格式（`docs/TEST_REPORT.md`）

```markdown
# TrustMem 功能验证报告
环境：python <ver> / TRUSTMEM_CRYPTO=<mock|real> / TRUSTMEM_SIGN=<...> / commit <hash>
日期：____   随机种子：____

## 一、总结论（三句以内）
1. 能跑通的：____
2. 有代码但测不出来的：____
3. 缺失或验证失败的：____

## 二、分层结果
| 层 | 项 | 结论 | 复现命令 | 证据 |
|---|---|---|---|---|
| L1 | 无占位符 | ✅ 0 处 | `grep -rn ...` | (输出片段) |
| L4 | I14 | ❌ | `pytest tests/test_i14_seq.py -k cross_task` | 期望 DENY，实际 ALLOW |

## 三、§4 十二项抽查
（逐条，同样三态 + 证据）

## 四、缺口清单（按严重程度排序）
每条写：现象 / 最小复现 / 影响到哪个论证或哪张表 / 建议补法（不要动手补）

## 五、停机记录
（S1–S5，每条附 grep 证据）

## 六、本轮没测到的
诚实列出。测不到的原因写清楚。
```

---

## §6 停机协议（照原设计，遇到就停，不要硬做）

| 触发 | 你必须输出 |
|---|---|
| **S1** 真实签名与文档不一致 | 两侧签名并列 + 三种改法及影响面 |
| **S2** 要调用的函数找不到 | grep 证据 + 它在哪一层被需要 |
| **S3** 你新写的测试让原有基线变红 | 变红断言 + 最小复现 + 判断是"基线假设了旧行为"还是"我测错了" |
| **S4** 某检查看起来只能靠 LLM 判断才能测 | 说明为什么格上偏序表达不了，**拿到答复前不要实现** |
| **S5** 你发现设计本身有洞 | 洞在哪 + 最小反例 + 建议补法（**这条最欢迎，优先报**） |

---

## §7 收尾：必须正面回答的七个问题

报告最后一节，逐条作答，不许绕：

1. `core/` 里到底有没有三水位、四值裁决、TaskScope、IngestMode？**有=贴代码行号，没有=直接说没有。**
2. `decrypt` 调用次数是否**恰好**等于 ALLOW 块数？绕过 PDP 直连密码服务能不能成功？
3. I1–I14 实际跑了多少组穷举、多少条随机序列？**0 违反是真的 0 还是有被 skip 的用例？**
4. TR1–TR16 有几条有真实代码支撑、有几条只在文档里？
5. 13 条攻击里，防护 ON 时**真正被规则硬拦**的有几条？A11 的日志里是不是两条不同的拒绝规则？
6. 八张表里有几个数字是实跑出来的、几个是占位符？表 14 的五项是不是分开报的？
7. **本轮里，有没有任何一处你为了让结果好看而调整了断言、跳过了用例、或者放宽了参数？** 有就写出来。

---

## 附：三条不许触碰的红线（每轮开头自己复述一遍）

1. 不修业务与安全逻辑，只新建测试。
2. 不为绿而绿。红的就报红的。
3. 测不出来就写"测不出来"，绝不填估算值。
